<div class="{{ $fieldInfo->size }}">
    <div class="mb-0">
        <div class="d-flex justify-content-end align-items-baseline">
            <button type="submit" id="{{ $fieldInfo->name }}" class="btn btn-success">{{ $fieldInfo->label }}</button>
        </div>
    </div>
</div>